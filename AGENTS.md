# AGENTS.md

This repository uses **`AGENT.md`** in the root as the primary project guide for coding agents.

If you are an automated agent, read:

- `AGENT.md` (root) for monorepo architecture and conventions
- `tren_webclient/example-app/AGENTS.md` for Laravel-specific rules inside that subproject

